✅ fix copilot (auto-trigger enabled)
✅ fix transparency (already working)
✅ add git tabs (shortcuts added)
✅ fix telescope grep (working with gof, got)
✅ remove margin (no colorcolumn)
✅ use ctrl z instead of u (added)
✅ add go to definition (gd, gD, gr, gi, K)
✅ add color scheme with the config (monokai working)
✅ add window title (configured in options)
✅ add format shortcut (<leader>cf)
✅ add shortcut to split window (<leader>wv, <leader>wh, <leader>wc)
✅ reduce tree explorer width (30 instead of 40)
- add spotify integration (TODO)
- add discord integration (TODO)
- add a shortcut manager (TODO)
✅ line number should not be relative in insert mode (auto-command added)
✅ implement a terminal (<leader>tt, <leader>tv, <leader>th)

